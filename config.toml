baseURL = "http://wafel34.github.io/first-blog"
languageCode = "en-us"
title = "Example Hugo Blog"
theme = "mainroad"
paginate = "10"
disqusShortname = "@disqus_SAFs3Hx0uL"
publishDir = "docs"


[Author] # Used in authorbox
    name = "Admin"
    bio = "Maybe he is a successful blogger or writer. Nobody knows it."
    avatar = "img/avatar.png"

[Params]
    subtitle = "Basic blog example" # Subtitle of your site. Used in site header
    description = " Basic blog based on a template, without any customisations." # Description of your site. Used in meta description
    opengraph = true # Enable OpenGraph if true
    readmore = true # Show "Read more" button in list if true
    leftsidebar = false # Move sidebar to the left side if true
    authorbox = true # Show authorbox at bottom of pages if true
    post_navigation = true # Show post navigation at bottom of pages if true
    postSections = ["post"] # the section pages to show on home page and the "Recent articles" widget
    #postSections = ["blog", "news"] # alternative that shows more than one section's pages
    #dateformat = "2006-01-02" # change the format of dates

[Params.widgets]
    search = false # Enable "Search" widget
    recent_articles = true # Enable "Recent articles" widget
    recent_articles_num = 5 # Set the number of articles in the "Recent articles" widget
    categories = true # Enable "Categories" widget
    tags = true # Enable "Tags" widget
    tags_counter = false # Enable counter for each tag in "Tags" widget (disabled by default)

    # Enable "Social" widget, if any of "social_*" set a value
    social_facebook = "username"
    social_twitter = "username"
    social_linkedin = "username"
    social_github = "username"
    social_email = "example@example.com"
    social_google_plus = "profileid"


[[menu.main]]
    name = "About"
    pre = "<i class='fa fa-heart'></i>"
    weight = -110
    identifier = "about"
    url = "/about"

[[menu.main]]
    name = "Home"
    pre = "<i class='fa fa-heart'></i>"
    weight = -120
    identifier = "home"
    url = "/"

[[menu.main]]
    name = "Contact"
    pre = "<i class='fa fa-heart'></i>"
    weight = -100
    identifier = "contact"
    url = "/contact"
